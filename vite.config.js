import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  base: '/DiscoverProducts/', // Make sure to include the leading and trailing slashes!
  // ... rest of your config
})
